{
  "name": "local-splendor-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @local-splendor/shared run build && pnpm -r --parallel --if-present run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
