{
  "name": "galaxore-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @galaxore/shared run build && pnpm -r --parallel --if-present run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
